import { StyleSheet } from "react-native";

export const pinStyle = StyleSheet.create({
    viewMarker: {
        height: 30,
        padding: 5,
        alignItems: 'center',
        justifyContent: 'center',
        borderRadius: 5
    },

    textoMarker: {
        color: '#FFF'
    }
});
